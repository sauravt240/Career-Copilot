# Career Copilot Environment Configuration
# Optional: Set at least one API key to enable live LLM generation.
# If no key is set, Career Copilot uses its built-in intelligent fallback engine.

GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Backend server host and port
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
